# tests/

Architecture Layer 4 — automated verification. Playwright/browser flows live in `tests/systemV2`. Unit tests for backend logic are co-located with their source in `backend/src/**` (e.g. `vendorIngestionService.test.ts` next to the service it tests).

Ref: CLAUDE.md → Architecture & System Layers (Layer 4); Testing & Validation Rules.
