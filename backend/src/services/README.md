# backend/src/services/

Business logic services (alumni, briefings, outreach agents, content generation, ingestion pipelines, etc.). One responsibility per module — a service file does one thing.

Never: route definitions, Sequelize model declarations, or UI code.

Ref: CLAUDE.md → Folder Responsibilities (`backend/src/services/`).
