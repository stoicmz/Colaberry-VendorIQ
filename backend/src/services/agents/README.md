# backend/src/services/agents/

Agent orchestration subtrees: openclaw, intelligence, marketing. Coordinates multiple services/steps for a single agent workflow.

Never: raw business logic that belongs in a plain service — orchestration only.

Ref: CLAUDE.md → Folder Responsibilities (`backend/src/services/agents/`).
